import React, { useState } from 'react'

const Favorites = () => {
    const [favs, setFavs] = useState(); 

    return (
        <div>
            FAVORITES
        </div>
    )
}

export default Favorites
